#!/bin/sh
echo "🔍 Running TypeScript check before push..."
cd "$(git rev-parse --show-toplevel)"
npx -p typescript tsc --noEmit --pretty 2>&1
if [ $? -ne 0 ]; then
  echo "❌ TypeScript errors found. Fix them before pushing."
  exit 1
fi
echo "✅ TypeScript check passed."